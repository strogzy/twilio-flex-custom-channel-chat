{
  "name": "viber",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "1.2.2",
    "axios": "^0.26.1",
    "base-64": "^1.0.0",
    "node-fetch": "^3.2.3",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "twilio-run": "^3.4.1"
  },
  "engines": {
    "node": "14"
  }
}
